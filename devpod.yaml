workspace:
  name: python-devpod-workspace
  provider:
    name: kubernetes
    options:
      podTemplate:
        volumes:
          - name: tmp-volume
            emptyDir: {}
        volumeMounts:
          - name: tmp-volume
            mountPath: /tmp
